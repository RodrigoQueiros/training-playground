pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
	    script {
	    	def buildConfigs = readJSON text: '[{name: "dev", command: "date"}, {name: "prod", command: "hostname"}]'
	    }
            steps {
                script {
                	def buildConfigs = []
                	buildConfigs.each{ buildConfig ->
                		sh "echo '$buildConfig.name' && $buildConfig.command"
					}
                }
            }
        }
    }
}
