pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('build') {
            steps {
                sh 'cat /etc/alpine-release'
		
		sh 'cat NewFile.txt > date'
		sh 'sleep 10s'
		sh 'cat NewFile.txt
            }
        }
    }
}
