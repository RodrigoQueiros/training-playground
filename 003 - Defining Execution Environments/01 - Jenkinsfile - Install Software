pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
	    	sh "apk update"
            	sh "apk-get install curl"
                sh "curl www.google.com"
            }
        }
    }
}
