pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
            	sudo apk-get install curl
                sh "curl www.google.com"
            }
        }
    }
}
